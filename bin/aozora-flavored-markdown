#!/usr/bin/env bash
# aozora-flavored-markdown CLI — native development wrapper.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$REPO_ROOT"

exec cargo run --locked --quiet --package aozora-flavored-markdown-cli -- "$@"
